import React from "react";
import { Prevalence } from "@/types/ElementTypes";

const Prevalence = ({
  universe,
  sun,
  ocean,
  human,
  earth_crust,
  meteorites,
}: Prevalence) => {
  return <div>Prevalence</div>;
};

export default Prevalence;
